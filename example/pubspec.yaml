name: verify_example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^0.14.0
  flutter_bloc: '>=7.0.0-nullsafety.5 < 8.0.0'
  verify:
    path: ../

dev_dependencies:
  freezed: ^0.14.0+1
  json_serializable: ^4.0.2
  build_runner:

flutter:
  uses-material-design: true
